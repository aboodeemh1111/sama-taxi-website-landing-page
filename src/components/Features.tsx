// Key Features Component
// Purpose: Highlight what makes your service stand out

const Features = () => {
  return (
    <section>
      {/* 
      Section Heading (h2): "Why Sama Taxi is Your Best Choice for Rides in KSA" (text-4xl)
      
      Feature Blocks (3-4 columns):
      1. "Professional Female Drivers" (text-2xl) + description (text-base)
      2. "24/7 Availability" (text-2xl) + description (text-base)
      3. "Transparent & Affordable Pricing" (text-2xl) + description (text-base)
      4. "Easy In-App Booking" (text-2xl) + description (text-base)
      
      Icons for each feature
      */}
    </section>
  );
};

export default Features;
